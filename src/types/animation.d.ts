/**
 * animation.d.ts
 * 
 * Responsibility:
 * TypeScript interfaces defining GSAP timeline data, animation configurations,
 * and keyframe dictionaries for complex choreographies.
 */

export interface AnimationConfig {
  duration: number;
  ease: string;
  delay?: number;
}
